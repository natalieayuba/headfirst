import React from 'react';
import { MdSearch } from 'react-icons/md';

const SearchButton = () => {
  return (
    <button>
      <MdSearch size={24} />
    </button>
  );
};

export default SearchButton;
